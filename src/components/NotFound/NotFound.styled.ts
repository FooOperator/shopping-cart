import styled from "styled-components"

const Container = styled.div`
    display: flex;
    align-self: center;
    margin: auto 0;
`;

const Notice = styled.p`
    font-size: 2rem;
`;

export default {
    Container,
    Notice
}